class post {
    constructor (titulo, autor, texto)
    {
        this.titulo = titulo;
        this.autor = autor;
        this.texto = texto;
    }

}

function criarPost()
{
    const meuPost = new post (titulo, autor, texto)
}