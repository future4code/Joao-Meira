export const numberArray: number[] = [1, 2, 3, 4, 5, 6]
const sum = numberArray.reduce((a:number, b: number) => a + b)

export type infoNumbers = {
    quantity: number,
    odd: number,
    even: number,
    arraySum: number,
}

function infoArrayNumbers (numberArray : number) : void {
    let quantity = numberArray.length
    let odd = 
}